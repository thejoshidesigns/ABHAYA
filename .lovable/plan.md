Plan: Connect this project to GitHub and create a new public repository named `AbhayaLovable`.

## What will happen

1. **You connect GitHub in the Lovable UI** (I cannot do this part for you — it requires your GitHub authorization):
   - Open the **Plus (+)** menu in the chat input → **GitHub** → **Connect project**.
   - Authorize the Lovable GitHub app when prompted.
   - Select your GitHub account/organization.
   - Enter repository name: `AbhayaLovable`.
   - Choose **Public** visibility.
   - Click **Create Repository**.

2. **I verify the repo is ready to receive the project** and make no unnecessary code changes. The project currently uses a clean TanStack Start template with placeholder content, so it will sync cleanly without needing structural fixes.

3. **After sync starts**, I update the placeholder `src/routes/index.tsx` content and replace generic meta tags in `src/routes/__root.tsx` with `AbhayaLovable` branding so the published repo and preview do not carry boilerplate "Lovable App" / "Your App" text.

## What I won't do
- I cannot click the GitHub connection buttons or authorize on your behalf — that must be done in your Lovable editor.
- I will not migrate the `Abhaya2` GitHub repo code here; if you still want that rebuilt after GitHub is connected, it will be a follow-up task.

## Expected outcome
- A new public repo at `github.com/<your-account>/AbhayaLovable`.
- Lovable ↔ GitHub two-way sync active, so future changes push automatically.
- Placeholder content replaced with `AbhayaLovable` naming.